#ifndef PHONEBOOK_HPP
# define PHONEBOOK_HPP
# include <iostream>
# include <string.h>
# include <iomanip>

using std::cout;
using std::cin;
using std::endl;

// enum Command {exit, add, search};

// enum Add_field {first_name, last_name, nickname, login, postal_address, email_address, phone_number, birthday, date, favorite_meal, underwear_color, darkest_secret};

// enum Search_field {index, first_name, last_name, nickname};


#endif
